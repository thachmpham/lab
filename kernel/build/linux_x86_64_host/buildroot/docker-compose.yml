export FORCE_UNSAFE_CONFIGURE=1
make -j `nproc`

cp /buildroot/output/images/rootfs.ext2 /spaceroot@pc:~/git/lab/kernel/build/linux_x86_64_host/buildroot# cat docker-compose.yml
services:
    buildroot:
        container_name: b1
        hostname: b1
        image: buildroot
        build: .
        volumes:
            - ~/space:/space
